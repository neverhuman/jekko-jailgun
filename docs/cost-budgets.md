# Cost Budgets

- Default CI and local proof lanes are zero-cost.
- No paid external service is required for `just fast`, `just test`, `just build`, or `bash ops/ci/security.sh`.
- Any future paid surface must document its budget, quota, and kill switch before it becomes part of the release lane.
